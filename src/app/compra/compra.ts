//import (Cliente) from './cliente/cliente';
//import (Boleta) from './boleta';

export class Compra {

    id: number;
    costoTotal: number;
    envio:boolean;
    fecha:Date;
    direccion:string;
    estado:boolean;
    //cliente:Cliente
    //boletas[]:Boleta
}